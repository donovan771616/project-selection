spring:
  datasource:
    driver-class-name: org.h2.Driver
    url: jdbc:h2:mem:cpt202_project_selection;MODE=MySQL;DATABASE_TO_LOWER=TRUE
    username: sa
    password:
  sql:
    init:
      mode: never
